<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myfirstapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- GCM -->
	<permission
	    android:name="br.com.hachitecnologia.gcm.permission.C2D_MESSAGE"
	    android:protectionLevel="signature" />
	<uses-permission android:name="br.com.hachitecnologia.gcm.permission.C2D_MESSAGE" />
	<!-- Permite o recebimento de mensagens do GCM. -->
	<uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
	<!-- Permite a conexão com o serviço GCM. -->
	<uses-permission android:name="android.permission.INTERNET" />
	<!-- O GCM requer uma conta do Google para sua utilização. -->
	<uses-permission android:name="android.permission.GET_ACCOUNTS" />
	<!-- Mantém o processador dormindo quando uma mensagem é recebida. -->
	<uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        
        <receiver
		  android:name="com.google.android.gcm.GCMBroadcastReceiver"
		  android:permission="com.google.android.c2dm.permission.SEND" >
		    <intent-filter>
		      <action android:name="com.google.android.c2dm.intent.RECEIVE" />
		      <action android:name="com.google.android.c2dm.intent.REGISTRATION" />
		      <category android:name="com.bruno.configuracao" />
		    </intent-filter>
		</receiver>
        
        <service android:name="com.bruno.configuracao.GCMIntentService" />
        
        <activity
            android:name="com.example.myfirstapp.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.myfirstapp.DisplayMessageActivity"
            android:label="@string/title_activity_display_message"
            android:parentActivityName="com.example.myfirstapp.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.myfirstapp.MainActivity" />
        </activity>
		<activity
		  android:name="com.bruno.activity.AtivaGcmActivity"
		  android:label="Google cloud Messaging">
		    <intent-filter>
		      <action android:name="android.intent.action.MAIN" />
		      <category android:name="android.intent.category.LAUNCHER" />
		    </intent-filter>
		</activity>
		<activity
		  	android:name="com.bruno.activity.MostraMensagemActivity"
		 	android:label="Mensagem Recebida" >
		</activity>
    </application>

</manifest>
